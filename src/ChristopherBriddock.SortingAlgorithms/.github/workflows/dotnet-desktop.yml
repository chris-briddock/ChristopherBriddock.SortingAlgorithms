# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Build & Restore
      run: dotnet build --configuration Release ChristopherBriddock.SortingAlgorithms.sln
    - name: Publish
      run: dotnet publish ChristopherBriddock.SortingAlgorithms/ChristopherBriddock.SortingAlgorithms.csproj -c Release
    - name: Create the package
      run: dotnet pack --configuration Release ChristopherBriddock.SortingAlgorithms/ChristopherBriddock.SortingAlgorithms.csproj
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3
      with:
        name: ChristopherBriddock.SortingAlgorithms
        path: ChristopherBriddock.SortingAlgorithms/bin/Release/net7.0/publish
        if-no-files-found: error
    - name: NuGet push
      run: dotnet nuget push "ChristopherBriddock.SortingAlgorithms/bin/Release/*.nupkg" -k ${{secrets.NUGET_API_KEY}} -s https://api.nuget.org/v3/index.json
